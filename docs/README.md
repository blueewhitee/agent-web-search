# Docs assets

Drop README images here. Two recommended shots (see main README):

| File | What to capture |
|---|---|
| `demo.png` | The `web_search` MCP tool firing inside Claude Desktop / Cursor / Pi — shows the ranked markdown result with cosine scores + source URLs. This is the hero image above the fold. |
| `curl-demo.png` | Terminal screenshot of the `curl -X POST localhost:8000/search ...` command and its ranked JSON response. Reproducible proof that goes near the API Reference. |

Capture tips:

- Crop to the relevant block, not the whole desktop.
- Use a dark theme + good font for terminal shots; syntax highlighting helps.
- PNG, not JPG, for crisp text.
- Once committed, the README references them as `docs/demo.png` and `docs/curl-demo.png` — no further edits needed.